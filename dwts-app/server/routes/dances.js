import express from 'express';

//import { } from '../controllers/dance.js';
//import auth from '../middleware/auth.js'; // need once card liking comes in

const router = express.Router();

//router.post('/action', auth, importedAction);

export default router;